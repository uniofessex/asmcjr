% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_wnom_coords.R
\name{plot_wnom_coords}
\alias{plot_wnom_coords}
\title{Plot W-NOMINATE Coordinates with Optional Confidence Intervals}
\usage{
plot_wnom_coords(
  obj,
  shapeVar = NULL,
  dropNV = FALSE,
  ptSize = 4,
  ci = FALSE,
  level = 0.95
)
}
\arguments{
\item{obj}{An object of class \code{wnominate} containing the results of a W-NOMINATE analysis, including legislators' ideal points and associated uncertainties.}

\item{shapeVar}{An optional variable used to differentiate groups of legislators by shape and color in the plot. If provided, the points will be shaped and colored according to this variable.}

\item{dropNV}{A logical value indicating whether to drop non-voters (NA values) from the plot. Default is \code{FALSE}.}

\item{ptSize}{A numeric value specifying the size of the points in the plot. Default is \code{4}.}

\item{ci}{A logical value indicating whether to include confidence ellipses around the points. Default is \code{FALSE}.}

\item{level}{A numeric value specifying the confidence level for the ellipses. Default is \code{0.95}.}
}
\value{
A \code{ggplot} object displaying the legislators' coordinates, optionally with confidence ellipses.
}
\description{
This function creates a scatter plot of legislators' ideal points based on W-NOMINATE analysis results. The plot can optionally include confidence ellipses around the points, representing uncertainty in the coordinates.
}
\examples{
\dontrun{
# Assuming `result` is a wnominate object and `legislator_data` contains a grouping variable
g <- plot_wnom_coords(result, dropNV = TRUE, ci = TRUE)
print(g)
}
}
